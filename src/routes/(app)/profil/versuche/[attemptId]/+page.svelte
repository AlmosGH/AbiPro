<script lang="ts">import type { PageProps } from './$types'; let { data }: PageProps = $props();</script>
<svelte:head><title>Versuch {data.detail.attempt.id} – AbiPro</title></svelte:head>
<main><p><a href="/profil">Zurück zum Profil</a></p><h1>{data.detail.attempt.kind === 'practice' ? 'Übungsergebnis' : 'Probeprüfungsergebnis'}</h1><p><strong>{data.detail.attempt.status === 'graded' ? `${data.detail.attempt.score} von ${data.detail.attempt.maxScore} Punkten` : 'Bewertung ausstehend'}</strong></p>
{#each data.detail.rows as row, index (`${row.title}-${index}`)}<article><h2>{row.title} · Version {row.version}</h2>{#if row.taskStatus === 'archived' || row.versionStatus === 'retired'}<p>Historische Aufgabenversion ({row.taskStatus === 'archived' ? 'archiviert' : 'zurückgezogen'})</p>{/if}<h3>{row.question}</h3><pre>{JSON.stringify(row.response, null, 2)}</pre><p>{row.answerStatus === 'needs_review' ? 'Selbstbewertung erforderlich' : `${row.awardedPoints ?? 0} / ${row.maximum} Punkte`}</p>{#if row.feedback}<p>{row.feedback}</p>{/if}</article>{/each}</main>
